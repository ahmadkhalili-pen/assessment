apiVersion: v1
kind: Service
metadata:
  name: mysql-primary-external
  namespace: db
spec:
  type: NodePort
  selector:
    statefulset.kubernetes.io/pod-name: mysql-cluster-primary-0
  ports:
    - port: 3306
      targetPort: 3306
      nodePort: 30306
